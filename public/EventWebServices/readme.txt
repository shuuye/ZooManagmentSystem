==================================================================
QR GENERATION (consume)
==================================================================
Step 1 (install libraries in cmd )
pip install Flask
pip install qrcode[pil]
c:\Windows\System32\my_flask_app>mysql-connector-python

step 2 (run the app.py file(outside of the apache) before run the generateqr.php to generate qr code)

==================================================================

==================================================================
Display the schedules in calender view (provide service)
==================================================================

Step 1 (install libraries in cmd )
pip install requests matplotlib
pip install pandas
pip install requests

Step 2 (run the get_user_events.php before run the test.py (outside of the apache))

==================================================================

